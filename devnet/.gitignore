# Generated by the devnet at runtime, never committed.
#
# .deployed.json is written fresh by every `npm run devnet:deploy`, and its addresses are only
# meaningful against the chain instance that produced them. .state/ is the cranker's run log,
# which the devnet server reads back.
.deployed.json
.state/
