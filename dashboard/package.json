{
  "name": "solstice-cranker-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mission Control — local demo/ops dashboard for the Solstice quarterly cranker.",
  "scripts": {
    "dev": "vite",
    "dev:fixtures": "VITE_USE_FIXTURES=1 vite",
    "build": "tsc -b && vite build",
    "build:fixtures": "VITE_USE_FIXTURES=1 tsc -b && VITE_USE_FIXTURES=1 vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --force"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
